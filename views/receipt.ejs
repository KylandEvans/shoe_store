<%- layout("layout/boilerplate") %>

<main class="container container-fluid mt-5">
	<h1>Your order is on it's way!</h1>
	<div class="w-100 container-fluid my-5">
		<!-- FIXME: Add breakpoint in CSS to break up height for small screens -->
		<div class="row">
			<div class="col-md-2">
				<h3>Payment</h3>
			</div>
			<div class="col-md-2">
				<h5>Items total </h5>
				<h5>Shipping Cost </h5>
				<h5>Estimated Tax </h5>
				<h3>Total </h3>
			</div>
			<div class="col-md-2 d-flex flex-column border-end">
				<h5>$ <%= newOrder.itemsTotal.toFixed(2) %></h5>
				<h5>$ <%= newOrder.shippingCost.toFixed(2) %></h5>
				<h5>$ <%= newOrder.orderTax.toFixed(2) %></h5>
				<h3>$ <%= newOrder.orderTotal.toFixed(2) %></h3>
			</div>
			<div class="col-md-2">
				<h3>Shipping</h3>
			</div>
			<div class="col-md-1"></div>
			<div class="col-md-2">
				<h5
					><%= newOrder.shippingAddress.address1 %> <%= newOrder.shippingAddress.address2 %>
				</h5>
				<h5><%= newOrder.shippingAddress.city %></h5>
				<h5><%= newOrder.shippingAddress.state %></h5>
				<h5><%= newOrder.shippingAddress.zipCode %></h5>
			</div>
		</div>
	</div>
	<% newOrder.items.forEach(item => { %>
	<div class="row">
		<div class="col-3 w-25">
			<img src="<%= item.item.image %>" alt="" class="img-thumbnail h-50" />
		</div>
		<div class="col-6">
			<h3><%= item.item.name %></h3>
			<h6><%= item.item.subTitle %></h6>
			<h6>Size: <%= item.size %> </h6>
			<h6>$ <%= item.item.price.toFixed(2) %></h6>
		</div>
	</div>

	<% }) %>

	<a href="/" class="btn btn-primary mb-5">Browse more products</a>
</main>

<%- include("./partials/cart") %>
